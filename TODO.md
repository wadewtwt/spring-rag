# Spring RAG Todo List

> 更新时间：2026-04-28
> 说明：本清单基于当前仓库状态整理，P0 已经补齐真实 Qwen 工作流主线，后续重点转到知识库能力、会话持久化和工程化。

## P0 当前主线

- [x] 将 `RagWorkflowService` 中的 `rewriter` 节点接成真实问题改写逻辑
- [x] 将 `generator` 节点接成真实 LLM 回答生成
- [x] 将 `evaluator` 节点升级成真实检索质量判定
- [x] 给状态机补上“重检索”分支，形成 `Retriever -> Evaluator -> Rewriter/Retry -> Generator/Fallback` 闭环
- [x] 将聊天入口切到新的工作流 Bean，并通过集成测试验证上传 -> 检索 -> 回答链路
- [x] 增加 `chroma` profile 下的集成测试骨架，只有检测到兼容的 Chroma V2 API 时才执行
- [ ] 在本机或测试环境真正跑通一次兼容 `langchain4j-chroma` 的 Chroma V2 服务联调

## P1 知识库能力

- [ ] 将当前“按行切片”升级成更合理的分块策略，例如按段落、窗口、重叠切片
- [ ] 补齐 PDF / Markdown / TXT 的真实解析链路，而不是只按原始文本字节处理
- [ ] 给文档和切片补元数据：文档 ID、文件名、页码、段落号、上传时间、租户或用户标识
- [ ] 增加文档列表、删除、重建索引接口，补齐知识库管理闭环
- [ ] 把 `Top-K`、最小分数阈值、集合名等参数真正接入检索实现，而不是只做配置暴露

## P1 对话与状态

- [ ] 将会话状态从内存仓库切换到数据库持久化，落地 `threadId + checkpoint/state`
- [ ] 把“记忆”从仅存消息历史升级为可恢复的图状态或会话快照
- [ ] 让多轮追问真正利用历史上下文，而不只是前端保留 `threadId`
- [ ] 定义失败次数的累积与清零规则

## P1 人工接管

- [ ] 把人工接管从关键词规则升级为明确策略模块，例如负面情绪、重复失败、低置信度
- [ ] 增加人工接管通知出口，例如 Webhook、邮件、企业微信、钉钉或工单系统
- [ ] 给前端补“已转人工 / 建议转人工”的稳定展示

## P1 前端体验

- [ ] 增加来源高亮、片段展开、来源跳转
- [ ] 补知识库上传/管理入口
- [ ] 在聊天页展示当前 RAG 模式，例如 `embedding / store / llm / retrieval`
- [ ] 处理 SSE 断流、超时、重试和取消发送
- [ ] 增加消息时间、系统消息样式、人工接管状态样式

## P2 工程与运维

- [ ] 补统一异常处理和稳定的错误响应结构
- [ ] 补 API 文档，例如 OpenAPI / Swagger
- [ ] 持续完善根目录 README 与启动说明
- [ ] 增加 CI，至少覆盖 `backend mvn test`、`frontend npm run test`、`frontend npm run build`
- [ ] 增加日志分级和关键链路埋点，方便排查检索、生成、回退、Chroma 连接问题
- [ ] 增加配置合法性校验，尤其是 `Chroma`、embedding 模式、集合名、DashScope 启用条件

## P2 安全与企业化

- [ ] 增加鉴权和接口访问控制
- [ ] 增加租户隔离和文档权限控制
- [ ] 增加上传文件大小、类型、内容安全校验
- [ ] 设计敏感数据脱敏、日志脱敏和私有文档访问边界

## P2 测试补强

- [x] 增加 `chroma` profile 集成测试入口
- [x] 增加状态机分支覆盖测试：重写、重检索、失败回退
- [ ] 增加人工接管触发链路覆盖
- [ ] 增加前端端到端联调测试
- [ ] 增加上传不同文档格式、空文档、超大文档、乱码文档的测试用例
