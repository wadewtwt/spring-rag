# Spring RAG Todo List

> 更新时间：2026-04-27  
> 说明：本清单基于当前仓库已完成的骨架、LangChain4j 接入、Chroma 配置化接入、LangGraph4j 最小工作流、前端最小聊天页整理而成。

## P0 当前主线缺口

- [ ] 把 `RagWorkflowService` 里的 `rewriter` 节点接成真实问题改写逻辑，而不是当前的占位透传。
- [ ] 把 `generator` 节点接成真实 LLM 回答生成，而不是当前直接拼接来源文本。
- [ ] 把 `evaluator` 节点升级成真实检索质量判定，而不是仅根据“是否有来源”二分。
- [ ] 给状态机补“重检索”分支，形成 `Retriever -> Evaluator -> Rewriter/Retry -> Generator` 的闭环。
- [ ] 真实跑通一次 `Chroma` 模式联调，验证文档上传、索引写入、检索召回和聊天回答全链路。

## P1 知识库能力

- [ ] 现在文档切片还是按“逐行切片”，需要改成更合理的分块策略：按段落、窗口、重叠切片。
- [ ] 补 PDF / Markdown / TXT 的真实解析链路，目前上传接口只处理原始文本字节。
- [ ] 给文档和切片补元数据：文档 ID、文件名、页码、段落号、上传时间、租户/用户标识。
- [ ] 增加文档列表、删除、重建索引接口，当前只有上传入口，没有文档管理闭环。
- [ ] 增加检索 Top-K、最小分数阈值、集合名等可配置项，避免参数写死在代码里。

## P1 对话与状态

- [ ] 当前会话状态仍是内存仓库，需要切到数据库持久化，落地 `threadId + checkpoint/state`。
- [ ] 把“记忆”从仅存消息历史，升级为真正可恢复的图状态或会话快照。
- [ ] 让多轮追问真正利用历史上下文，而不只是前端保留 `threadId`。
- [ ] 定义失败次数的累计与清零规则，当前人工接管相关状态还比较轻。

## P1 人工接管

- [ ] 把人工接管从关键词规则升级为明确的策略模块：负面情绪、重复失败、低置信度等。
- [ ] 增加人工接管通知出口，比如 Webhook、邮件、企业微信、钉钉或工单系统。
- [ ] 给前端补“已转人工/建议转人工”的稳定展示，而不只是临时提示文案。

## P1 前端体验

- [ ] 当前前端能展示来源，但还没有“来源高亮、展开片段、点击跳转”这些可用能力。
- [ ] 补充文档上传页面或上传面板，现在前端还没有知识库管理入口。
- [ ] 给聊天页增加当前 RAG 模式展示，例如显示 `embedding/store/profile` 状态。
- [ ] 处理 SSE 断流、超时、重试和取消发送，当前错误处理还是最小版本。
- [ ] 增加消息时间、系统消息样式、人工接管状态样式等更清晰的会话表现。

## P2 工程与运维

- [ ] 给后端补统一异常处理和稳定的错误响应结构。
- [ ] 补 API 文档，比如 OpenAPI / Swagger。
- [ ] 增加启动说明和根目录 README 完整使用指南，现在还缺少统一入口文档。
- [ ] 增加 CI：至少包含 `backend mvn test`、`frontend npm run test`、`frontend npm run build`。
- [ ] 增加日志分级和关键链路埋点，方便排查检索、生成、回退、Chroma 连接问题。
- [ ] 增加配置项校验，尤其是 `Chroma`、embedding 模式、集合名等配置的合法性校验。

## P2 安全与企业化

- [ ] 增加鉴权和接口访问控制。
- [ ] 增加租户隔离和文档权限控制。
- [ ] 增加上传文件大小、类型、内容安全校验。
- [ ] 设计敏感数据脱敏、日志脱敏和私有文档访问边界。

## P2 测试补强

- [ ] 增加 `chroma` profile 下的集成测试，当前主要还是 `inmemory/simple` 友好测试。
- [ ] 增加状态机分支覆盖测试：重写、重检索、失败回退、人工接管触发。
- [ ] 增加前端端到端联调测试，而不是只停留在组件测试。
- [ ] 增加上传不同文档格式、空文档、超大文档、乱码文档的测试用例。

## 备注

- [ ] 当前项目已经有这些基础：Spring Boot 骨架、最小前端、LangChain4j embedding/store 接口、Chroma 配置接入、LangGraph4j 最小工作流、RAG 状态查询接口。
- [ ] 现在最值得优先做的是：`真实 LLM 节点 + 状态机闭环 + Chroma 实际联调 + 数据库存档`。
