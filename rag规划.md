## 需求规格说明书 (Requirements.md)

### 1. 项目背景

构建一套企业级的智能 AI 客服系统，旨在通过检索增强生成 (RAG) 技术，使 AI 能够基于私有知识库（PDF、Markdown 等）提供专业、准确的回答，并具备复杂的多轮对话处理能力。

### 2. 核心功能需求

- **私有知识库管理**：支持上传业务手册、操作指南等文档，并自动进行切片与向量化存储。
- **智能问答 (RAG)**：AI 需严格根据检索到的知识片段回答问题，避免产生“幻觉”。
- **多轮对话上下文管理**：系统需识别用户追问（如“它保修多久？”中的“它”），并结合历史语境进行补全。
- **状态持久化（记忆功能）**：支持对话存档与读档。即使用户断线或重启，AI 仍能记得之前的沟通进度。
- **逻辑自纠错**：当检索到的资料不足以回答问题时，AI 应能自我反思并尝试重新检索，而非胡编乱造。
- **人工接管触发**：当识别到用户负面情绪或 AI 连续无法解决问题时，自动挂起流程并通知人工接管。

### 3. 非功能性需求

- **交互体验**：前端需支持流式渲染（打字机效果），并提供参考来源标注。
- **数据安全**：核心数据支持本地化部署，确保企业私有文档不泄露。
- **可扩展性**：采用状态机架构，便于后期增加复杂业务逻辑分支。

------

## 🏗️ 技术架构设计文档 (Architecture.md)

### 1. 逻辑架构 (Logic Layer)

系统采用 **“状态机 (State Machine)”** 模式，由 LangGraph 驱动核心决策流程。

- **输入节点 (Input)**：接收用户提问及 `thread_id`。
- **检索节点 (Retriever)**：将问题向量化后，从向量数据库检索 Top-K 知识片段。
- **改写节点 (Rewriter)**：针对模糊提问进行上下文补全。
- **评估节点 (Evaluator)**：判断检索内容的相关性，决定是进入生成环节还是重新检索。
- **生成节点 (Generator)**：结合知识片段与历史状态，调用 LLM 生成最终回复。

### 2. 技术栈选型 (Technology Stack)

| **维度**       | **推荐选型**        | **说明**                                  |
| -------------- | ------------------- | ----------------------------------------- |
| **开发语言**   | Java (JDK 17+)      | 利用 Java 全栈背景，保证系统稳定性。      |
| **AI 框架**    | LangChain4j         | 负责 LLM 调用、Embedding 转换及工具集成。 |
| **逻辑编排**   | LangGraph4j         | 负责带状态的循环逻辑与分支控制。          |
| **向量数据库** | ChromaDB / Milvus   | 存储文档向量，支持语义检索。              |
| **业务数据库** | MySQL / PostgreSQL  | 存储业务数据及对话存档。                  |
| **存档组件**   | JdbcCheckpointSaver | 实现基于数据库的对话状态持久化。          |
| **后端框架**   | Spring Boot         | 提供 API 网关、限流及安全控制。           |

### 3. 数据存储架构 (Data Storage)

- **向量库 (Vector Store)**：存储 `TextSegment`（文字片段）及其对应的向量数据。
- **存档表 (Checkpoints Table)**：
  - `thread_id`：隔离不同用户的会话。
  - `checkpoint_blob`：以二进制形式存储整个 State 对象快照。
  - `parent_id`：记录父子关系，支持对话回滚与逻辑回溯。

### 4. 系统运行流程

1. **用户请求** $\rightarrow$ **Java 后端**（鉴权/限流）。
2. **LangGraph** $\rightarrow$ 从 **MySQL** 读取 `parent_id` 对应的存档状态。
3. **检索逻辑** $\rightarrow$ 查询 **ChromaDB** 获取知识点。
4. **模型决策** $\rightarrow$ 调用 **Claude/GPT/Ollama** 生成回复。
5. **状态保存** $\rightarrow$ 自动更新 **MySQL** 存档。
6. **结果返回** $\rightarrow$ 前端 **SSE 流式展示**。